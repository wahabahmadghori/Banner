import React from 'react'
import {Page} from '@shopify/polaris'

const Install = ({history}) => {
    console.log(history)
    return (
        <Page>
            Install Script
        </Page>
    )
}

export default Install
